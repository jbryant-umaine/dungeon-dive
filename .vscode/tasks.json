{
    "tasks": [
        {
            "label": "Build with Make",
            "type": "shell",
            "command": "make",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}" // Set the working directory to the current file's directory
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true // Set this as the default build task
            },
            "detail": "Run make to build the project."
        }
    ],
    "version": "2.0.0"
}